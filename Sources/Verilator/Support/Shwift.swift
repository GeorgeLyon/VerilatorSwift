import Shwift
import SystemPackage

extension Shwift.Context {
  func execute(
    _ executablePath: FilePath,
    _ arguments: String...,
    environment: Environment,
    in directory: FilePath,
    outputTo: inout TextOutputStream
  ) async throws {

  }
}
